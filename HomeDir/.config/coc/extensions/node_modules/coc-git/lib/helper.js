"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const coc_nvim_1 = require("coc.nvim");
function getUrl(remote, branch, filepath, lines) {
    let uri = remote.replace(/\.git$/, '');
    if (uri.startsWith('git')) {
        let str = uri.slice(4);
        let parts = str.split(':', 2);
        uri = `https://${parts[0]}/${parts[1]}`;
    }
    let u = coc_nvim_1.Uri.parse(uri);
    if (u.authority.startsWith('github.com')) {
        const anchor = lines ? lines.map(l => `L${l}`).join('-') : '';
        return uri + '/blob/' + branch + '/' + filepath + (anchor ? '#' + anchor : '');
    }
    coc_nvim_1.workspace.showMessage(`Can't get url form: ${u.authority}`);
    return '';
}
exports.getUrl = getUrl;
//# sourceMappingURL=helper.js.map