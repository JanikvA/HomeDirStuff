import { Document } from 'coc.nvim';
import { Diff } from './types';
export declare function parseDiff(diffStr: string): Diff[];
export declare function getDiff(root: string, doc: Document): Promise<Diff[]>;
