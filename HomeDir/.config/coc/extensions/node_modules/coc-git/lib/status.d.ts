interface Decorator {
    changedDecorator: string;
    conflictedDecorator: string;
    stagedDecorator: string;
    untrackedDecorator: string;
}
export declare function gitStatus(cwd: string, character: string, decorator: Decorator): Promise<string>;
export {};
